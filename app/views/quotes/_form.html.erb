<%= form_for(@quote) do |f| %>
  <% if @quote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quote.errors.count, "error") %> prohibited this quote from being saved:</h2>

      <ul>
      <% @quote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field panel-primary" id="edit">
    <div class="panel-heading"><%= f.label :author %></div><br>
    <%= f.text_field :author %>
  </div>
  <div class="field panel-primary" id="edit">
    <div class="panel-heading"><%= f.label :quote %></div><br>
    <%= f.text_area :quote %>
  </div>
  <div class="field panel-primary" id="edit">
    <div class="panel-heading"><%= f.label :background %></div><br>
    <%= f.text_area :background %>
  </div>
  <div class="field">
    <%= f.submit class: "btn btn-success" %>
  </div>
<% end %>
